<div class="menu-catalog">
    Listed in Brand: <%= link_to "Catalog", catalog_path, :class => "menu-category-link" %>
  >
  <% if @product.nil? %>
    <%= link_to @brand.Name, brand_path(@brand.id), :class => "current-category-link", :onclick =>"javascript:void(0);" %>
  <% else %>
    <%= link_to @brand.Name, brand_path(@brand.id), :class => "menu-category-link" %>
  <% end %>
</div>